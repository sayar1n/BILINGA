import React from 'react';
import styles from './InfoSide.module.scss';

const InfoSide = () => {
  return (
    <div className={styles.infoSide}>
      {/* Здесь будет размещаться контент */}
    </div>
  );
};

export default InfoSide;
